//! Chapter 1 — variables: shared helpers and topic modules (`ex01_*`, `ex02_*`, …).
//! Each `src/bin/*.rs` binary calls `run()` on one topic module.

pub mod ex01_bindings;
pub mod ex02_numeric;
pub mod ex03_shadow_const;
pub mod ex04_tuples;

pub mod helpers;
