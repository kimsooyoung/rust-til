[package]
name = "project_notes"
version = "0.1.0"
edition = "2024"
description = "Tiny CLI that appends a wrapped local-time marker to a text file."
license = "MIT OR Apache-2.0"
readme = "README.md"

[dependencies]
chrono = "0.4.42"
anyhow = "1.0"
